# Copy this file to .env and update with your values
# DO NOT commit .env to version control

# Debug Panel Access (Production)
# Set a secret key to access debug panel in production via ?debug=YOUR_KEY
# Leave empty or remove to disable debug panel in production
VITE_DEBUG_KEY=your-secret-debug-key-here

# Geocoding API Configuration
# For local development, the Vite dev server proxy is used automatically
# For production, the Netlify edge function proxy is used automatically
# Only set this if you want to use a custom proxy URL
VITE_GEOCODING_PROXY_URL=

# Other environment variables (if needed)
# VITE_APP_VERSION=1.0.0
# VITE_ANALYTICS_ENDPOINT=https://your-analytics-endpoint.com
# VITE_ENABLE_ANALYTICS=true